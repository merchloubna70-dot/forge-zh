#!/usr/bin/env bash
# Forge-zh.app 主可执行文件
# 调用 ~/.local/bin/forge-zh-launch 启动三栏工作台

LAUNCH="$HOME/.local/bin/forge-zh-launch"

if [[ -x "$LAUNCH" ]]; then
    exec "$LAUNCH"
else
    osascript -e 'display alert "Forge-zh 未正确安装" message "请先挂载 Forge-zh DMG，双击 install.sh 完成安装，再启动 Forge-zh。"'
fi
